<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data>
        
        <!-- Vue Liste pour les étapes des signalements -->
        <record id="view_whistleblowing_stage_list" model="ir.ui.view">
            <field name="name">whistleblowing.alert.stage.list</field>
            <field name="model">whistleblowing.alert.stage</field>
            <field name="arch" type="xml">
                <list editable="bottom">
                    <field name="sequence" widget="handle"/>
                    <field name="name"/>
                    <field name="description"/>
                    <field name="is_closed"/>
                    <field name="fold"/>
                    <field name="color" widget="color_picker"/>
                    <field name="alert_count"/>
                </list>
            </field>
        </record>

        <!-- Vue Formulaire pour les étapes des signalements -->
        <record id="view_whistleblowing_stage_form" model="ir.ui.view">
            <field name="name">whistleblowing.alert.stage.form</field>
            <field name="model">whistleblowing.alert.stage</field>
            <field name="arch" type="xml">
                <form>
                    <sheet>
                        <div class="oe_button_box" name="button_box">
                            <button class="oe_stat_button" type="object" name="action_view_alerts" icon="fa-exclamation-triangle">
                                <field name="alert_count" widget="statinfo" string="Alertes"/>
                            </button>
                        </div>
                        
                        <group>
                            <group>
                                <field name="name"/>
                                <field name="sequence"/>
                                <field name="color" widget="color_picker"/>
                            </group>
                            <group>
                                <field name="is_closed"/>
                                <field name="fold"/>
                            </group>
                        </group>
                        
                        <group string="Description">
                            <field name="description" nolabel="1"/>
                        </group>
                    </sheet>
                </form>
            </field>
        </record>

        <!-- Action pour les étapes des signalements -->
        <record id="action_whistleblowing_stage" model="ir.actions.act_window">
            <field name="name">Étapes des Signalements</field>
            <field name="res_model">whistleblowing.alert.stage</field>
            <field name="view_mode">list,form</field>
            <field name="groups_id" eval="[(4, ref('group_whistleblowing_manager'))]"/>
            <field name="help" type="html">
                <p class="o_view_nocontent_smiling_face">
                    Configurer les étapes du workflow
                </p>
                <p>
                    Définissez les étapes par lesquelles passent les signalements d'alerte.
                </p>
            </field>
        </record>

    </data>
</odoo>